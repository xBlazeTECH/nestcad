<!DOCTYPE html>
<html lang="en">
    <head>
        {{> header }}
    </head>
    <body>
        <header>
            {{> navbar }}
            {{> scroller }}
        </header>
        {{ message }}
        {{> scripts }}
    </body>
</html>